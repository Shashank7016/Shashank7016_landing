{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "react/no-unescaped-entities": "warn", // Change to "off" or "error" if needed
    "no-unused-vars": "warn", // Example of another rule you might want to adjust
    // Add more rules as needed
  }
}
